// Define the valid roles based on your project requirements
export const validRoles = ['Admin', 'Dean', 'Registrar'];

// Since Zod is removed, these constants are now the primary export of this file.

// Note: If you later decide to add basic client-side validation, you would implement
// those functions here using plain JavaScript, not Zod.
